# Loaded by `vite --mode test` (Playwright e2e). Points the ui at the test api on :4001 instead of
# the dev api on :4000. Overrides VITE_DOMAIN from .env because `.env.test` takes precedence.
VITE_DOMAIN=http://localhost:4001/api
